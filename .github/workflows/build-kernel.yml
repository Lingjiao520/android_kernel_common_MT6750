name: Build MT6763 Kernel (Final)

on: workflow_dispatch

jobs:
  prepare:
    runs-on: ubuntu-22.04
    steps:
      - name: 安装系统包
        run: |
          sudo apt update
          sudo apt install -y build-essential flex bison libncurses-dev \
                              libssl-dev bc device-tree-compiler \
                              gcc-9 g++-9 libc6-dev-i386
          sudo add-apt-repository -y ppa:deadsnakes/ppa
          sudo apt install -y python2

      - name: 获取交叉编译器（带缓存）
        uses: actions/cache@v4
        id: cache-gcc
        with:
          path: gcc
          key: gcc49-${{ runner.os }}-${{ hashFiles('.github/workflows/*.yml') }}
          restore-keys: |
            gcc49-${{ runner.os }}-

      - name: 下载交叉编译器（缓存未命中时）
        if: steps.cache-gcc.outputs.cache-hit != 'true'
        run: git clone --depth=1 https://github.com/LineageOS/android_prebuilts_gcc_linux-x86_aarch64_aarch64-linux-android-4.9 gcc

      - name: 上传工具链
        uses: actions/upload-artifact@v4
        with:
          name: toolchain
          path: gcc/

  build:
    needs: prepare
    runs-on: ubuntu-22.04
    steps:
      - name: 检出源码
        uses: actions/checkout@v4

      - name: 安装编译基础包及 Python 2
        run: |
          sudo apt update
          sudo apt install -y build-essential flex bison libncurses-dev \
                              libssl-dev bc device-tree-compiler gcc-9 g++-9
          sudo add-apt-repository -y ppa:deadsnakes/ppa
          sudo apt install -y python2
          # 确保 python2 可用
          which python2

      - name: 下载交叉编译器
        uses: actions/download-artifact@v4
        with:
          name: toolchain
          path: gcc

      - name: 修复交叉编译器
        run: |
          chmod -R +x gcc
          sudo ln -sf $(pwd)/gcc/bin/* /usr/local/bin/

      - name: 确认 DrvGen.py 存在并赋予权限
        run: |
          ls -la tools/dct/DrvGen.py
          sudo chmod -R 777 tools/dct

      - name: 将 DrvGen 调用改为 python2
        run: |
          # 查找所有 Makefile 中硬编码的 DrvGen.py 调用，统一改为 python2
          grep -rl "DrvGen.py" scripts/ arch/ --include="*.mk" --include="Makefile" | while read f; do
            sed -i 's|\./tools/dct/DrvGen.py|python2 ./tools/dct/DrvGen.py|g' "$f"
            sed -i 's|DrvGen.py|python2 ./tools/dct/DrvGen.py|g' "$f"   # 以防有相对路径的其他写法
          done

      - name: 修复 DTC 及 extract-cert 的编译错误
        run: |
          echo 'HOST_EXTRACFLAGS += -fcommon' >> scripts/dtc/Makefile
          sed -i 's/-Werror//g' scripts/dtc/Makefile
          sed -i 's/-Werror//g' scripts/Makefile.host 2>/dev/null || true
          sed -i 's/-Werror//g' scripts/Makefile 2>/dev/null || true

      - name: 下载缺失的 SELinux 头文件
        run: |
          mkdir -p security/selinux/include
          for f in flask.h av_permissions.h classmap.h; do
            if [ ! -f security/selinux/include/$f ]; then
              curl -sL -o security/selinux/include/$f \
                "https://raw.githubusercontent.com/aosp-mirror/kernel_common/android-4.9/security/selinux/include/$f"
            fi
          done

      - name: 加载 defconfig
        run: |
          export ARCH=arm64
          export CROSS_COMPILE=aarch64-linux-android-
          export HOSTCC=gcc-9
          export HOSTCXX=g++-9
          make mrproper
          make k63v2_64_bsp_defconfig

      - name: 开启 KALLSYMS_ALL 并兼容旧编译器
        run: |
          sed -i 's/# CONFIG_KALLSYMS_ALL/CONFIG_KALLSYMS_ALL=y/' .config
          sed -i 's/CONFIG_CC_STACKPROTECTOR_STRONG=y/# CONFIG_CC_STACKPROTECTOR_STRONG is not set/' .config
          sed -i 's/# CONFIG_CC_STACKPROTECTOR_NONE is not set/CONFIG_CC_STACKPROTECTOR_NONE=y/' .config

      - name: 编译内核
        run: |
          export ARCH=arm64
          export CROSS_COMPILE=aarch64-linux-android-
          export HOSTCC=gcc-9
          export HOSTCXX=g++-9
          export PYTHON=python2
          make -j$(nproc) Image.gz-dtb
          cp arch/arm64/boot/Image.gz-dtb vmlinuz

      - name: 上传成品内核
        uses: actions/upload-artifact@v4
        with:
          name: MT6763-boot
          path: |
            arch/arm64/boot/Image.gz-dtb
            vmlinuz
